<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <include
        android:id="@+id/toolbar_wrapper"
        layout="@layout/fragment_base_toolbar"
        />

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/base_padding"
        >

        <com.rengwuxian.materialedittext.MaterialEditText
            android:id="@+id/username_field"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textEmailAddress"
            android:maxLines="1"
            android:hint="@string/field_email"
            app:met_primaryColor="@color/accent"
            app:met_floatingLabel="highlight"
            app:met_textColor="@color/text_primary"
            app:met_textColorHint="@color/text_hint_for_dark"
            app:met_helperTextColor="@color/text_hint_for_dark"
            app:met_floatingLabelTextColor="@color/text_hint_for_dark"
            app:met_underlineColor="@color/text_hint_for_dark"
            />

        <com.rengwuxian.materialedittext.MaterialEditText
            android:id="@+id/password_field"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textPassword"
            android:maxLines="1"
            android:hint="@string/field_password"
            app:met_primaryColor="@color/accent"
            app:met_floatingLabel="highlight"
            app:met_textColor="@color/text_primary"
            app:met_textColorHint="@color/text_hint_for_dark"
            app:met_helperTextColor="@color/text_hint_for_dark"
            app:met_floatingLabelTextColor="@color/text_hint_for_dark"
            app:met_underlineColor="@color/text_hint_for_dark"
            />

        <Button
            android:id="@+id/btn_login"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/title_login"
            android:layout_marginTop="@dimen/base_space"
            style="@style/button_rectangle"
            />
    </LinearLayout>

</LinearLayout>
